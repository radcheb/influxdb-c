{
  "name": "influxdb-c",
  "version": "0.0.2",
  "repo": "radcheb/influxdb-c",
  "description": "A C driver for influxDB, until version 0.8",
  "keywords": ["influxdb", "driver", "metric"],
  "license": "MIT",
  "install": "./bootstrap && ./configure && make && sudo make install"
}